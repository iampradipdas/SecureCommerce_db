# Flyway Database Configuration

# ✅ Change localhost → postgres (Docker service name)
flyway.url=jdbc:postgresql://postgres:5432/mydb
flyway.user=myuser
flyway.password=mypassword

# ✅ Change to Docker container path
flyway.locations=filesystem:/flyway/sql

# Schema
flyway.schemas=public

# Table used by Flyway to track migrations
flyway.table=flyway_schema_history

# Baseline for existing database
flyway.baselineOnMigrate=true

# Validate migrations before running
flyway.validateOnMigrate=true

# Encoding
flyway.encoding=UTF-8